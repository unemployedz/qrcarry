{
  "name": "qrcarry",
  "version": "3.0.0",
  "private": true,
  "description": "QR Carry web service with local QR decoding and a compatibility web proxy",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
